import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color(0xff8E6CEF);

  static const fillColorLightMode = Color(0xffF4F4F4);
  static const fillColorDarkMode = Color(0xff342F3F);

  static const lightModeBackgroundColor = Color(0xffFFFFFF);
  static const darkModeBackgroundColor = Color(0xff1D182A);

  static const primaryTextColorLightMode = Color(0xff272727);
  static const primaryTextColorDarkMode = Color(0xffFFFFFF);

  static const hintTextColorLightMode = Color(0xff8E8E8E);
  static const hintTextColorDarkMode = Color(0xff99979F);
}
